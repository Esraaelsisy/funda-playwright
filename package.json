{
  "name": "funda-playwright",
  "version": "1.0.0",
  "description": "This is a public repo for Funda Assignment using Playwright",
  "main": "index.js",
  "scripts": {
    "smokeTests": "npx playwright test --grep @smokeTests",
    "smokeTests:chromium": "npx playwright test --grep @smokeTests --project=chromium",
    "smokeTests:firefox": "npx playwright test --grep @smokeTests --project=firefox",
    "smokeTests:webkit": "npx playwright test --grep @smokeTests --project=webkit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.6"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
